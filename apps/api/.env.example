# PostgreSQL connection string (use crm_app role — non-superuser, respects RLS)
DATABASE_URL="postgresql://crm_app:crm_app_password@localhost:5432/crm_dev"

# API server port
PORT=3001

# Secret key for Better Auth (min 32 chars)
BETTER_AUTH_SECRET=""

# Auth base URL (API server URL)
BETTER_AUTH_URL="http://localhost:3001"

# Frontend URL for CORS
WEB_URL="http://localhost:3000"
